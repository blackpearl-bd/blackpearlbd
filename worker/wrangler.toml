name = "blackpearl-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Secrets are set via: wrangler secret put <KEY> --name blackpearl-api
# Do NOT add empty vars here — use wrangler secret instead

# R2 bucket (optional, for file storage)
# [[r2_buckets]]
# binding = "BLACKPEARL_BUCKET"
# bucket_name = "blackpearl-assets"
